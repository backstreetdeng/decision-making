
## 12:36 P0 修复：chat.html 拿不到完整报告

### 现象
- 大管家 10:29:30 -> 10:53:29 跑了 chat.html 端到端测试（test_e2e_1782613770）
- 测试通过，但 chat.html 渲染区显示「未返回报告内容」
- 只有 success=true 和 quality_passed=true 元数据，没有报告正文

### 根因
- FastAPI 18003 适配器的 _complete_payload 只从 gateway_result.get('text') 提取报告
- 	ext 来自我（market_strategy）的 LLM reply，但 LLM 偶尔只回元数据摘要（confidence / quality_passed）
- 即便 strategy-orchestrator 已经通过 callback 推过完整报告，前端只读 complete 事件的 
eport，所以拿不到

### 修复
1. **P0 代码层** astapi_18003_adapter/main.py
   - 新增 _looks_like_metadata_only_report(text)：检测 gateway text 是否只是元数据
   - 新增 _aggregate_report_from_callbacks(session_id)：扫描 session_manager 事件历史，取最长 
eport / nswer / markdown 作为兜底
   - _complete_payload 改 async，新增 session_id 参数；metadata-only 时自动聚合
2. **P1 提示层** SOUL.md 通道回信策略
   - 加了「chat.html 回信硬约束」小节，明确 5 条必须 / 禁止规则
   - 给合规 / 违规示例
3. **测试** 	ests/test_fastapi_18003_adapter.py
   - 加 9 个新单测，覆盖 heuristic 与 aggregation
   - 14/14 PASS

### 验证
- python -m unittest tests.test_fastapi_18003_adapter -v -> 14/14 OK
- py_compile 全部通过
- 不影响其他适配层行为

### 下一步
- 等编排专家修完后重跑端到端测试
- 把测试结果发给大管家
